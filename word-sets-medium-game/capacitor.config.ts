import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Word Sets Medium Game',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
